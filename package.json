{
    "name": "fitness",
    "version": "1.0.0",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev --parallel",
        "lint": "turbo run lint",
        "test": "vitest",
        "clean": "turbo run clean && rm -rf node_modules && rm -rf .turbo"
    },
    "dependencies": {}
}
